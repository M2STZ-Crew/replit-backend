# public/

Drop the RepLiT logo here as **`logo.png`** (transparent background looks best on
the dark theme). The `Logo` component renders `/logo.png` and falls back to a
styled "RepLiT" wordmark if the file is missing.
